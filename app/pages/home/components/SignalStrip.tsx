/** Strip janji produk — dekoratif, tanpa state. */
export function SignalStrip() {
	return (
		<section className="signal-strip" aria-label="Product promises">
			<span>Thoughtful by default</span>
			<i />
			<span>Useful from the first click</span>
			<i />
			<span>Human at every scale</span>
		</section>
	);
}
